#include "Fixed.class.hpp"
#include <iostream>

Fixed   eval_expr(std::string exp)
{
    
}

int main(int argc, char const *argv[])
{
    if (argc == 2)
    {
        std::string expr = std::string(argv[1]);
        size_t      i = 0;

    }
    return 0;
}
